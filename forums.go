// Code generated by Fern. DO NOT EDIT.

package whopsdk

import (
	json "encoding/json"
	fmt "fmt"
	internal "github.com/whopio/whopsdk-go/internal"
	big "math/big"
)

var (
	listForumsRequestFieldAfter     = big.NewInt(1 << 0)
	listForumsRequestFieldBefore    = big.NewInt(1 << 1)
	listForumsRequestFieldFirst     = big.NewInt(1 << 2)
	listForumsRequestFieldLast      = big.NewInt(1 << 3)
	listForumsRequestFieldProductID = big.NewInt(1 << 4)
	listForumsRequestFieldAccountID = big.NewInt(1 << 5)
)

type ListForumsRequest struct {
	// Returns the elements in the list that come after the specified cursor.
	After *string `json:"-" url:"after,omitempty"`
	// Returns the elements in the list that come before the specified cursor.
	Before *string `json:"-" url:"before,omitempty"`
	// Returns the first _n_ elements from the list.
	First *int `json:"-" url:"first,omitempty"`
	// Returns the last _n_ elements from the list.
	Last *int `json:"-" url:"last,omitempty"`
	// The unique identifier of a product to filter by. When set, only forums connected to this product are returned.
	ProductID *string `json:"-" url:"product_id,omitempty"`
	// The unique identifier of the company to list forums for.
	AccountID string `json:"-" url:"account_id"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`
}

func (l *ListForumsRequest) require(field *big.Int) {
	if l.explicitFields == nil {
		l.explicitFields = big.NewInt(0)
	}
	l.explicitFields.Or(l.explicitFields, field)
}

// SetAfter sets the After field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListForumsRequest) SetAfter(after *string) {
	l.After = after
	l.require(listForumsRequestFieldAfter)
}

// SetBefore sets the Before field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListForumsRequest) SetBefore(before *string) {
	l.Before = before
	l.require(listForumsRequestFieldBefore)
}

// SetFirst sets the First field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListForumsRequest) SetFirst(first *int) {
	l.First = first
	l.require(listForumsRequestFieldFirst)
}

// SetLast sets the Last field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListForumsRequest) SetLast(last *int) {
	l.Last = last
	l.require(listForumsRequestFieldLast)
}

// SetProductID sets the ProductID field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListForumsRequest) SetProductID(productID *string) {
	l.ProductID = productID
	l.require(listForumsRequestFieldProductID)
}

// SetAccountID sets the AccountID field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListForumsRequest) SetAccountID(accountID string) {
	l.AccountID = accountID
	l.require(listForumsRequestFieldAccountID)
}

var (
	retrieveForumsRequestFieldID = big.NewInt(1 << 0)
)

type RetrieveForumsRequest struct {
	// The unique identifier of the forum or experience to retrieve.
	ID string `json:"-" url:"-"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`
}

func (r *RetrieveForumsRequest) require(field *big.Int) {
	if r.explicitFields == nil {
		r.explicitFields = big.NewInt(0)
	}
	r.explicitFields.Or(r.explicitFields, field)
}

// SetID sets the ID field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (r *RetrieveForumsRequest) SetID(id string) {
	r.ID = id
	r.require(retrieveForumsRequestFieldID)
}

// A discussion forum where members can create posts, comment, and react, belonging to an experience.
var (
	forumFieldEmailNotificationPreference = big.NewInt(1 << 0)
	forumFieldExperience                  = big.NewInt(1 << 1)
	forumFieldID                          = big.NewInt(1 << 2)
	forumFieldWhoCanComment               = big.NewInt(1 << 3)
	forumFieldWhoCanPost                  = big.NewInt(1 << 4)
)

type Forum struct {
	// The email notification setting that controls which posts trigger email alerts. One of: all_admin_posts, only_weekly_summary, none.
	EmailNotificationPreference ForumEmailNotificationPreferences `json:"email_notification_preference" url:"email_notification_preference"`
	// The parent experience that this forum belongs to.
	Experience *ForumExperience `json:"experience" url:"experience"`
	// The unique identifier for the entity
	ID string `json:"id" url:"id"`
	// The permission level controlling who can comment on posts. One of: everyone, admins.
	WhoCanComment ForumWhoCanCommentTypes `json:"who_can_comment" url:"who_can_comment"`
	// The permission level controlling who can create new posts. One of: everyone, admins.
	WhoCanPost ForumWhoCanPostTypes `json:"who_can_post" url:"who_can_post"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`

	extraProperties map[string]interface{}
	rawJSON         json.RawMessage
}

func (f *Forum) GetEmailNotificationPreference() ForumEmailNotificationPreferences {
	if f == nil {
		return ""
	}
	return f.EmailNotificationPreference
}

func (f *Forum) GetExperience() *ForumExperience {
	if f == nil {
		return nil
	}
	return f.Experience
}

func (f *Forum) GetID() string {
	if f == nil {
		return ""
	}
	return f.ID
}

func (f *Forum) GetWhoCanComment() ForumWhoCanCommentTypes {
	if f == nil {
		return ""
	}
	return f.WhoCanComment
}

func (f *Forum) GetWhoCanPost() ForumWhoCanPostTypes {
	if f == nil {
		return ""
	}
	return f.WhoCanPost
}

func (f *Forum) GetExtraProperties() map[string]interface{} {
	if f == nil {
		return nil
	}
	return f.extraProperties
}

func (f *Forum) require(field *big.Int) {
	if f.explicitFields == nil {
		f.explicitFields = big.NewInt(0)
	}
	f.explicitFields.Or(f.explicitFields, field)
}

// SetEmailNotificationPreference sets the EmailNotificationPreference field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *Forum) SetEmailNotificationPreference(emailNotificationPreference ForumEmailNotificationPreferences) {
	f.EmailNotificationPreference = emailNotificationPreference
	f.require(forumFieldEmailNotificationPreference)
}

// SetExperience sets the Experience field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *Forum) SetExperience(experience *ForumExperience) {
	f.Experience = experience
	f.require(forumFieldExperience)
}

// SetID sets the ID field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *Forum) SetID(id string) {
	f.ID = id
	f.require(forumFieldID)
}

// SetWhoCanComment sets the WhoCanComment field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *Forum) SetWhoCanComment(whoCanComment ForumWhoCanCommentTypes) {
	f.WhoCanComment = whoCanComment
	f.require(forumFieldWhoCanComment)
}

// SetWhoCanPost sets the WhoCanPost field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *Forum) SetWhoCanPost(whoCanPost ForumWhoCanPostTypes) {
	f.WhoCanPost = whoCanPost
	f.require(forumFieldWhoCanPost)
}

func (f *Forum) UnmarshalJSON(data []byte) error {
	type unmarshaler Forum
	var value unmarshaler
	if err := json.Unmarshal(data, &value); err != nil {
		return err
	}
	*f = Forum(value)
	extraProperties, err := internal.ExtractExtraProperties(data, *f)
	if err != nil {
		return err
	}
	f.extraProperties = extraProperties
	f.rawJSON = json.RawMessage(data)
	return nil
}

func (f *Forum) MarshalJSON() ([]byte, error) {
	type embed Forum
	var marshaler = struct {
		embed
	}{
		embed: embed(*f),
	}
	explicitMarshaler := internal.HandleExplicitFields(marshaler, f.explicitFields)
	return json.Marshal(explicitMarshaler)
}

func (f *Forum) String() string {
	if f == nil {
		return "<nil>"
	}
	if len(f.rawJSON) > 0 {
		if value, err := internal.StringifyJSON(f.rawJSON); err == nil {
			return value
		}
	}
	if value, err := internal.StringifyJSON(f); err == nil {
		return value
	}
	return fmt.Sprintf("%#v", f)
}

// Email notification preference option for a forum feed
type ForumEmailNotificationPreferences string

const (
	ForumEmailNotificationPreferencesAllAdminPosts     ForumEmailNotificationPreferences = "all_admin_posts"
	ForumEmailNotificationPreferencesOnlyWeeklySummary ForumEmailNotificationPreferences = "only_weekly_summary"
	ForumEmailNotificationPreferencesNone              ForumEmailNotificationPreferences = "none"
)

func NewForumEmailNotificationPreferencesFromString(s string) (ForumEmailNotificationPreferences, error) {
	switch s {
	case "all_admin_posts":
		return ForumEmailNotificationPreferencesAllAdminPosts, nil
	case "only_weekly_summary":
		return ForumEmailNotificationPreferencesOnlyWeeklySummary, nil
	case "none":
		return ForumEmailNotificationPreferencesNone, nil
	}
	var t ForumEmailNotificationPreferences
	return "", fmt.Errorf("%s is not a valid %T", s, t)
}

func (f ForumEmailNotificationPreferences) Ptr() *ForumEmailNotificationPreferences {
	return &f
}

// The parent experience that this forum belongs to.
var (
	forumExperienceFieldID       = big.NewInt(1 << 0)
	forumExperienceFieldIsPublic = big.NewInt(1 << 1)
	forumExperienceFieldName     = big.NewInt(1 << 2)
)

type ForumExperience struct {
	// The unique identifier for the experience.
	ID string `json:"id" url:"id"`
	// Whether this experience is publicly visible to all users, including those without a membership.
	IsPublic bool `json:"is_public" url:"is_public"`
	// The display name of this experience shown to users in the product navigation. Maximum 255 characters.
	Name string `json:"name" url:"name"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`

	extraProperties map[string]interface{}
	rawJSON         json.RawMessage
}

func (f *ForumExperience) GetID() string {
	if f == nil {
		return ""
	}
	return f.ID
}

func (f *ForumExperience) GetIsPublic() bool {
	if f == nil {
		return false
	}
	return f.IsPublic
}

func (f *ForumExperience) GetName() string {
	if f == nil {
		return ""
	}
	return f.Name
}

func (f *ForumExperience) GetExtraProperties() map[string]interface{} {
	if f == nil {
		return nil
	}
	return f.extraProperties
}

func (f *ForumExperience) require(field *big.Int) {
	if f.explicitFields == nil {
		f.explicitFields = big.NewInt(0)
	}
	f.explicitFields.Or(f.explicitFields, field)
}

// SetID sets the ID field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *ForumExperience) SetID(id string) {
	f.ID = id
	f.require(forumExperienceFieldID)
}

// SetIsPublic sets the IsPublic field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *ForumExperience) SetIsPublic(isPublic bool) {
	f.IsPublic = isPublic
	f.require(forumExperienceFieldIsPublic)
}

// SetName sets the Name field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *ForumExperience) SetName(name string) {
	f.Name = name
	f.require(forumExperienceFieldName)
}

func (f *ForumExperience) UnmarshalJSON(data []byte) error {
	type unmarshaler ForumExperience
	var value unmarshaler
	if err := json.Unmarshal(data, &value); err != nil {
		return err
	}
	*f = ForumExperience(value)
	extraProperties, err := internal.ExtractExtraProperties(data, *f)
	if err != nil {
		return err
	}
	f.extraProperties = extraProperties
	f.rawJSON = json.RawMessage(data)
	return nil
}

func (f *ForumExperience) MarshalJSON() ([]byte, error) {
	type embed ForumExperience
	var marshaler = struct {
		embed
	}{
		embed: embed(*f),
	}
	explicitMarshaler := internal.HandleExplicitFields(marshaler, f.explicitFields)
	return json.Marshal(explicitMarshaler)
}

func (f *ForumExperience) String() string {
	if f == nil {
		return "<nil>"
	}
	if len(f.rawJSON) > 0 {
		if value, err := internal.StringifyJSON(f.rawJSON); err == nil {
			return value
		}
	}
	if value, err := internal.StringifyJSON(f); err == nil {
		return value
	}
	return fmt.Sprintf("%#v", f)
}

// A discussion forum where members can create posts, comment, and react, belonging to an experience.
var (
	forumListItemFieldEmailNotificationPreference = big.NewInt(1 << 0)
	forumListItemFieldExperience                  = big.NewInt(1 << 1)
	forumListItemFieldID                          = big.NewInt(1 << 2)
	forumListItemFieldWhoCanComment               = big.NewInt(1 << 3)
	forumListItemFieldWhoCanPost                  = big.NewInt(1 << 4)
)

type ForumListItem struct {
	// The email notification setting that controls which posts trigger email alerts. One of: all_admin_posts, only_weekly_summary, none.
	EmailNotificationPreference ForumEmailNotificationPreferences `json:"email_notification_preference" url:"email_notification_preference"`
	// The parent experience that this forum belongs to.
	Experience *ForumListItemExperience `json:"experience" url:"experience"`
	// The unique identifier for the entity
	ID string `json:"id" url:"id"`
	// The permission level controlling who can comment on posts. One of: everyone, admins.
	WhoCanComment ForumWhoCanCommentTypes `json:"who_can_comment" url:"who_can_comment"`
	// The permission level controlling who can create new posts. One of: everyone, admins.
	WhoCanPost ForumWhoCanPostTypes `json:"who_can_post" url:"who_can_post"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`

	extraProperties map[string]interface{}
	rawJSON         json.RawMessage
}

func (f *ForumListItem) GetEmailNotificationPreference() ForumEmailNotificationPreferences {
	if f == nil {
		return ""
	}
	return f.EmailNotificationPreference
}

func (f *ForumListItem) GetExperience() *ForumListItemExperience {
	if f == nil {
		return nil
	}
	return f.Experience
}

func (f *ForumListItem) GetID() string {
	if f == nil {
		return ""
	}
	return f.ID
}

func (f *ForumListItem) GetWhoCanComment() ForumWhoCanCommentTypes {
	if f == nil {
		return ""
	}
	return f.WhoCanComment
}

func (f *ForumListItem) GetWhoCanPost() ForumWhoCanPostTypes {
	if f == nil {
		return ""
	}
	return f.WhoCanPost
}

func (f *ForumListItem) GetExtraProperties() map[string]interface{} {
	if f == nil {
		return nil
	}
	return f.extraProperties
}

func (f *ForumListItem) require(field *big.Int) {
	if f.explicitFields == nil {
		f.explicitFields = big.NewInt(0)
	}
	f.explicitFields.Or(f.explicitFields, field)
}

// SetEmailNotificationPreference sets the EmailNotificationPreference field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *ForumListItem) SetEmailNotificationPreference(emailNotificationPreference ForumEmailNotificationPreferences) {
	f.EmailNotificationPreference = emailNotificationPreference
	f.require(forumListItemFieldEmailNotificationPreference)
}

// SetExperience sets the Experience field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *ForumListItem) SetExperience(experience *ForumListItemExperience) {
	f.Experience = experience
	f.require(forumListItemFieldExperience)
}

// SetID sets the ID field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *ForumListItem) SetID(id string) {
	f.ID = id
	f.require(forumListItemFieldID)
}

// SetWhoCanComment sets the WhoCanComment field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *ForumListItem) SetWhoCanComment(whoCanComment ForumWhoCanCommentTypes) {
	f.WhoCanComment = whoCanComment
	f.require(forumListItemFieldWhoCanComment)
}

// SetWhoCanPost sets the WhoCanPost field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *ForumListItem) SetWhoCanPost(whoCanPost ForumWhoCanPostTypes) {
	f.WhoCanPost = whoCanPost
	f.require(forumListItemFieldWhoCanPost)
}

func (f *ForumListItem) UnmarshalJSON(data []byte) error {
	type unmarshaler ForumListItem
	var value unmarshaler
	if err := json.Unmarshal(data, &value); err != nil {
		return err
	}
	*f = ForumListItem(value)
	extraProperties, err := internal.ExtractExtraProperties(data, *f)
	if err != nil {
		return err
	}
	f.extraProperties = extraProperties
	f.rawJSON = json.RawMessage(data)
	return nil
}

func (f *ForumListItem) MarshalJSON() ([]byte, error) {
	type embed ForumListItem
	var marshaler = struct {
		embed
	}{
		embed: embed(*f),
	}
	explicitMarshaler := internal.HandleExplicitFields(marshaler, f.explicitFields)
	return json.Marshal(explicitMarshaler)
}

func (f *ForumListItem) String() string {
	if f == nil {
		return "<nil>"
	}
	if len(f.rawJSON) > 0 {
		if value, err := internal.StringifyJSON(f.rawJSON); err == nil {
			return value
		}
	}
	if value, err := internal.StringifyJSON(f); err == nil {
		return value
	}
	return fmt.Sprintf("%#v", f)
}

// The parent experience that this forum belongs to.
var (
	forumListItemExperienceFieldID       = big.NewInt(1 << 0)
	forumListItemExperienceFieldIsPublic = big.NewInt(1 << 1)
	forumListItemExperienceFieldName     = big.NewInt(1 << 2)
)

type ForumListItemExperience struct {
	// The unique identifier for the experience.
	ID string `json:"id" url:"id"`
	// Whether this experience is publicly visible to all users, including those without a membership.
	IsPublic bool `json:"is_public" url:"is_public"`
	// The display name of this experience shown to users in the product navigation. Maximum 255 characters.
	Name string `json:"name" url:"name"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`

	extraProperties map[string]interface{}
	rawJSON         json.RawMessage
}

func (f *ForumListItemExperience) GetID() string {
	if f == nil {
		return ""
	}
	return f.ID
}

func (f *ForumListItemExperience) GetIsPublic() bool {
	if f == nil {
		return false
	}
	return f.IsPublic
}

func (f *ForumListItemExperience) GetName() string {
	if f == nil {
		return ""
	}
	return f.Name
}

func (f *ForumListItemExperience) GetExtraProperties() map[string]interface{} {
	if f == nil {
		return nil
	}
	return f.extraProperties
}

func (f *ForumListItemExperience) require(field *big.Int) {
	if f.explicitFields == nil {
		f.explicitFields = big.NewInt(0)
	}
	f.explicitFields.Or(f.explicitFields, field)
}

// SetID sets the ID field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *ForumListItemExperience) SetID(id string) {
	f.ID = id
	f.require(forumListItemExperienceFieldID)
}

// SetIsPublic sets the IsPublic field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *ForumListItemExperience) SetIsPublic(isPublic bool) {
	f.IsPublic = isPublic
	f.require(forumListItemExperienceFieldIsPublic)
}

// SetName sets the Name field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (f *ForumListItemExperience) SetName(name string) {
	f.Name = name
	f.require(forumListItemExperienceFieldName)
}

func (f *ForumListItemExperience) UnmarshalJSON(data []byte) error {
	type unmarshaler ForumListItemExperience
	var value unmarshaler
	if err := json.Unmarshal(data, &value); err != nil {
		return err
	}
	*f = ForumListItemExperience(value)
	extraProperties, err := internal.ExtractExtraProperties(data, *f)
	if err != nil {
		return err
	}
	f.extraProperties = extraProperties
	f.rawJSON = json.RawMessage(data)
	return nil
}

func (f *ForumListItemExperience) MarshalJSON() ([]byte, error) {
	type embed ForumListItemExperience
	var marshaler = struct {
		embed
	}{
		embed: embed(*f),
	}
	explicitMarshaler := internal.HandleExplicitFields(marshaler, f.explicitFields)
	return json.Marshal(explicitMarshaler)
}

func (f *ForumListItemExperience) String() string {
	if f == nil {
		return "<nil>"
	}
	if len(f.rawJSON) > 0 {
		if value, err := internal.StringifyJSON(f.rawJSON); err == nil {
			return value
		}
	}
	if value, err := internal.StringifyJSON(f); err == nil {
		return value
	}
	return fmt.Sprintf("%#v", f)
}

// Who can comment on a forum feed
type ForumWhoCanCommentTypes string

const (
	ForumWhoCanCommentTypesEveryone ForumWhoCanCommentTypes = "everyone"
	ForumWhoCanCommentTypesAdmins   ForumWhoCanCommentTypes = "admins"
)

func NewForumWhoCanCommentTypesFromString(s string) (ForumWhoCanCommentTypes, error) {
	switch s {
	case "everyone":
		return ForumWhoCanCommentTypesEveryone, nil
	case "admins":
		return ForumWhoCanCommentTypesAdmins, nil
	}
	var t ForumWhoCanCommentTypes
	return "", fmt.Errorf("%s is not a valid %T", s, t)
}

func (f ForumWhoCanCommentTypes) Ptr() *ForumWhoCanCommentTypes {
	return &f
}

// Who can post on a forum feed
type ForumWhoCanPostTypes string

const (
	ForumWhoCanPostTypesEveryone ForumWhoCanPostTypes = "everyone"
	ForumWhoCanPostTypesAdmins   ForumWhoCanPostTypes = "admins"
)

func NewForumWhoCanPostTypesFromString(s string) (ForumWhoCanPostTypes, error) {
	switch s {
	case "everyone":
		return ForumWhoCanPostTypesEveryone, nil
	case "admins":
		return ForumWhoCanPostTypesAdmins, nil
	}
	var t ForumWhoCanPostTypes
	return "", fmt.Errorf("%s is not a valid %T", s, t)
}

func (f ForumWhoCanPostTypes) Ptr() *ForumWhoCanPostTypes {
	return &f
}

// The connection type for ForumFeed.
var (
	listForumsResponseFieldData     = big.NewInt(1 << 0)
	listForumsResponseFieldPageInfo = big.NewInt(1 << 1)
)

type ListForumsResponse struct {
	// A list of nodes.
	Data []*ForumListItem `json:"data" url:"data"`
	// Information to aid in pagination.
	PageInfo *PageInfo `json:"page_info" url:"page_info"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`

	extraProperties map[string]interface{}
	rawJSON         json.RawMessage
}

func (l *ListForumsResponse) GetData() []*ForumListItem {
	if l == nil {
		return nil
	}
	return l.Data
}

func (l *ListForumsResponse) GetPageInfo() *PageInfo {
	if l == nil {
		return nil
	}
	return l.PageInfo
}

func (l *ListForumsResponse) GetExtraProperties() map[string]interface{} {
	if l == nil {
		return nil
	}
	return l.extraProperties
}

func (l *ListForumsResponse) require(field *big.Int) {
	if l.explicitFields == nil {
		l.explicitFields = big.NewInt(0)
	}
	l.explicitFields.Or(l.explicitFields, field)
}

// SetData sets the Data field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListForumsResponse) SetData(data []*ForumListItem) {
	l.Data = data
	l.require(listForumsResponseFieldData)
}

// SetPageInfo sets the PageInfo field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListForumsResponse) SetPageInfo(pageInfo *PageInfo) {
	l.PageInfo = pageInfo
	l.require(listForumsResponseFieldPageInfo)
}

func (l *ListForumsResponse) UnmarshalJSON(data []byte) error {
	type unmarshaler ListForumsResponse
	var value unmarshaler
	if err := json.Unmarshal(data, &value); err != nil {
		return err
	}
	*l = ListForumsResponse(value)
	extraProperties, err := internal.ExtractExtraProperties(data, *l)
	if err != nil {
		return err
	}
	l.extraProperties = extraProperties
	l.rawJSON = json.RawMessage(data)
	return nil
}

func (l *ListForumsResponse) MarshalJSON() ([]byte, error) {
	type embed ListForumsResponse
	var marshaler = struct {
		embed
	}{
		embed: embed(*l),
	}
	explicitMarshaler := internal.HandleExplicitFields(marshaler, l.explicitFields)
	return json.Marshal(explicitMarshaler)
}

func (l *ListForumsResponse) String() string {
	if l == nil {
		return "<nil>"
	}
	if len(l.rawJSON) > 0 {
		if value, err := internal.StringifyJSON(l.rawJSON); err == nil {
			return value
		}
	}
	if value, err := internal.StringifyJSON(l); err == nil {
		return value
	}
	return fmt.Sprintf("%#v", l)
}

// The banner image displayed at the top of the forum page. Pass null to remove the existing banner.
var (
	updateForumsRequestBannerImageFieldID = big.NewInt(1 << 0)
)

type UpdateForumsRequestBannerImage struct {
	// The ID of an existing file object.
	ID string `json:"id" url:"id"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`

	extraProperties map[string]interface{}
	rawJSON         json.RawMessage
}

func (u *UpdateForumsRequestBannerImage) GetID() string {
	if u == nil {
		return ""
	}
	return u.ID
}

func (u *UpdateForumsRequestBannerImage) GetExtraProperties() map[string]interface{} {
	if u == nil {
		return nil
	}
	return u.extraProperties
}

func (u *UpdateForumsRequestBannerImage) require(field *big.Int) {
	if u.explicitFields == nil {
		u.explicitFields = big.NewInt(0)
	}
	u.explicitFields.Or(u.explicitFields, field)
}

// SetID sets the ID field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (u *UpdateForumsRequestBannerImage) SetID(id string) {
	u.ID = id
	u.require(updateForumsRequestBannerImageFieldID)
}

func (u *UpdateForumsRequestBannerImage) UnmarshalJSON(data []byte) error {
	type unmarshaler UpdateForumsRequestBannerImage
	var value unmarshaler
	if err := json.Unmarshal(data, &value); err != nil {
		return err
	}
	*u = UpdateForumsRequestBannerImage(value)
	extraProperties, err := internal.ExtractExtraProperties(data, *u)
	if err != nil {
		return err
	}
	u.extraProperties = extraProperties
	u.rawJSON = json.RawMessage(data)
	return nil
}

func (u *UpdateForumsRequestBannerImage) MarshalJSON() ([]byte, error) {
	type embed UpdateForumsRequestBannerImage
	var marshaler = struct {
		embed
	}{
		embed: embed(*u),
	}
	explicitMarshaler := internal.HandleExplicitFields(marshaler, u.explicitFields)
	return json.Marshal(explicitMarshaler)
}

func (u *UpdateForumsRequestBannerImage) String() string {
	if u == nil {
		return "<nil>"
	}
	if len(u.rawJSON) > 0 {
		if value, err := internal.StringifyJSON(u.rawJSON); err == nil {
			return value
		}
	}
	if value, err := internal.StringifyJSON(u); err == nil {
		return value
	}
	return fmt.Sprintf("%#v", u)
}

var (
	updateForumsRequestFieldID                          = big.NewInt(1 << 0)
	updateForumsRequestFieldBannedWords                 = big.NewInt(1 << 1)
	updateForumsRequestFieldBannerImage                 = big.NewInt(1 << 2)
	updateForumsRequestFieldEmailNotificationPreference = big.NewInt(1 << 3)
	updateForumsRequestFieldWhoCanComment               = big.NewInt(1 << 4)
	updateForumsRequestFieldWhoCanPost                  = big.NewInt(1 << 5)
)

type UpdateForumsRequest struct {
	// The unique identifier of the forum to update. Accepts either an experience ID (e.g. 'exp_xxxxx') or a forum ID.
	ID string `json:"-" url:"-"`
	// A list of words that are automatically blocked from posts in this forum. For example, ['spam', 'scam'].
	BannedWords []string `json:"banned_words,omitempty" url:"-"`
	// The banner image displayed at the top of the forum page. Pass null to remove the existing banner.
	BannerImage *UpdateForumsRequestBannerImage `json:"banner_image,omitempty" url:"-"`
	// Controls how email notifications are sent to members when new posts are created in this forum.
	EmailNotificationPreference *ForumEmailNotificationPreferences `json:"email_notification_preference,omitempty" url:"-"`
	// Controls which roles are allowed to comment on posts in this forum.
	WhoCanComment *ForumWhoCanCommentTypes `json:"who_can_comment,omitempty" url:"-"`
	// Controls which roles are allowed to create new posts in this forum.
	WhoCanPost *ForumWhoCanPostTypes `json:"who_can_post,omitempty" url:"-"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`
}

func (u *UpdateForumsRequest) require(field *big.Int) {
	if u.explicitFields == nil {
		u.explicitFields = big.NewInt(0)
	}
	u.explicitFields.Or(u.explicitFields, field)
}

// SetID sets the ID field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (u *UpdateForumsRequest) SetID(id string) {
	u.ID = id
	u.require(updateForumsRequestFieldID)
}

// SetBannedWords sets the BannedWords field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (u *UpdateForumsRequest) SetBannedWords(bannedWords []string) {
	u.BannedWords = bannedWords
	u.require(updateForumsRequestFieldBannedWords)
}

// SetBannerImage sets the BannerImage field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (u *UpdateForumsRequest) SetBannerImage(bannerImage *UpdateForumsRequestBannerImage) {
	u.BannerImage = bannerImage
	u.require(updateForumsRequestFieldBannerImage)
}

// SetEmailNotificationPreference sets the EmailNotificationPreference field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (u *UpdateForumsRequest) SetEmailNotificationPreference(emailNotificationPreference *ForumEmailNotificationPreferences) {
	u.EmailNotificationPreference = emailNotificationPreference
	u.require(updateForumsRequestFieldEmailNotificationPreference)
}

// SetWhoCanComment sets the WhoCanComment field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (u *UpdateForumsRequest) SetWhoCanComment(whoCanComment *ForumWhoCanCommentTypes) {
	u.WhoCanComment = whoCanComment
	u.require(updateForumsRequestFieldWhoCanComment)
}

// SetWhoCanPost sets the WhoCanPost field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (u *UpdateForumsRequest) SetWhoCanPost(whoCanPost *ForumWhoCanPostTypes) {
	u.WhoCanPost = whoCanPost
	u.require(updateForumsRequestFieldWhoCanPost)
}

func (u *UpdateForumsRequest) UnmarshalJSON(data []byte) error {
	type unmarshaler UpdateForumsRequest
	var body unmarshaler
	if err := json.Unmarshal(data, &body); err != nil {
		return err
	}
	*u = UpdateForumsRequest(body)
	return nil
}

func (u *UpdateForumsRequest) MarshalJSON() ([]byte, error) {
	type embed UpdateForumsRequest
	var marshaler = struct {
		embed
	}{
		embed: embed(*u),
	}
	explicitMarshaler := internal.HandleExplicitFields(marshaler, u.explicitFields)
	return json.Marshal(explicitMarshaler)
}
