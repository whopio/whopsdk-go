// Code generated by Fern. DO NOT EDIT.

package experiences_test

import (
	bytes "bytes"
	context "context"
	json "encoding/json"
	http "net/http"
	os "os"
	testing "testing"

	require "github.com/stretchr/testify/require"
	whopsdk "github.com/whopio/whopsdk-go"
	client "github.com/whopio/whopsdk-go/client"
	option "github.com/whopio/whopsdk-go/option"
)

func VerifyRequestCount(
	t *testing.T,
	testId string,
	method string,
	urlPath string,
	queryParams map[string]any,
	expected int,
) {
	wiremockURL := os.Getenv("WIREMOCK_URL")
	if wiremockURL == "" {
		wiremockURL = "http://localhost:8080"
	}
	WiremockAdminURL := wiremockURL + "/__admin"
	var reqBody bytes.Buffer
	reqBody.WriteString(`{"method":"`)
	reqBody.WriteString(method)
	reqBody.WriteString(`","urlPath":"`)
	reqBody.WriteString(urlPath)
	reqBody.WriteString(`","headers":{"X-Test-Id":{"equalTo":"`)
	reqBody.WriteString(testId)
	reqBody.WriteString(`"}}`)
	if len(queryParams) > 0 {
		reqBody.WriteString(`,"queryParameters":{`)
		first := true
		for key, value := range queryParams {
			if !first {
				reqBody.WriteString(",")
			}
			reqBody.WriteString(`"`)
			reqBody.WriteString(key)
			switch v := value.(type) {
			case string:
				reqBody.WriteString(`":{"equalTo":"`)
				reqBody.WriteString(v)
				reqBody.WriteString(`"}`)
			case []string:
				reqBody.WriteString(`":{"hasExactly":[`)
				for i, item := range v {
					if i > 0 {
						reqBody.WriteString(",")
					}
					reqBody.WriteString(`{"equalTo":"`)
					reqBody.WriteString(item)
					reqBody.WriteString(`"}`)
				}
				reqBody.WriteString(`]}`)
			}
			first = false
		}
		reqBody.WriteString("}")
	}
	reqBody.WriteString("}")
	resp, err := http.Post(WiremockAdminURL+"/requests/find", "application/json", &reqBody)
	require.NoError(t, err)
	var result struct {
		Requests []interface{} `json:"requests"`
	}
	json.NewDecoder(resp.Body).Decode(&result)
	require.Equal(t, expected, len(result.Requests))
}

func TestExperiencesListWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewWhop(
		option.WithBaseURL(WireMockBaseURL),
		option.WithToken("test-token"),
	)
	request := &whopsdk.ListExperiencesRequest{
		First: whopsdk.Int(
			42,
		),
		Last: whopsdk.Int(
			42,
		),
		CompanyID: "biz_xxxxxxxxxxxxxx",
		ProductID: whopsdk.String(
			"prod_xxxxxxxxxxxxx",
		),
		AppID: whopsdk.String(
			"app_xxxxxxxxxxxxxx",
		),
		CreatedBefore: whopsdk.Time(
			whopsdk.MustParseDateTime(
				"2023-12-01T05:00:00Z",
			),
		),
		CreatedAfter: whopsdk.Time(
			whopsdk.MustParseDateTime(
				"2023-12-01T05:00:00Z",
			),
		),
	}
	_, invocationErr := client.Experiences.List(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestExperiencesListWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestExperiencesListWithWireMock", "GET", "/experiences", map[string]interface{}{"first": "42", "last": "42", "company_id": "biz_xxxxxxxxxxxxxx", "product_id": "prod_xxxxxxxxxxxxx", "app_id": "app_xxxxxxxxxxxxxx", "created_before": "2023-12-01T05:00:00.000Z", "created_after": "2023-12-01T05:00:00.000Z"}, 1)
}

func TestExperiencesCreateWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewWhop(
		option.WithBaseURL(WireMockBaseURL),
		option.WithToken("test-token"),
	)
	request := &whopsdk.CreateExperiencesRequest{
		AppID:     "app_xxxxxxxxxxxxxx",
		CompanyID: "biz_xxxxxxxxxxxxxx",
	}
	_, invocationErr := client.Experiences.Create(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestExperiencesCreateWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestExperiencesCreateWithWireMock", "POST", "/experiences", nil, 1)
}

func TestExperiencesRetrieveWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewWhop(
		option.WithBaseURL(WireMockBaseURL),
		option.WithToken("test-token"),
	)
	request := &whopsdk.RetrieveExperiencesRequest{
		ID: "exp_xxxxxxxxxxxxxx",
	}
	_, invocationErr := client.Experiences.Retrieve(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestExperiencesRetrieveWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestExperiencesRetrieveWithWireMock", "GET", "/experiences/exp_xxxxxxxxxxxxxx", nil, 1)
}

func TestExperiencesDeleteWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewWhop(
		option.WithBaseURL(WireMockBaseURL),
		option.WithToken("test-token"),
	)
	request := &whopsdk.DeleteExperiencesRequest{
		ID: "exp_xxxxxxxxxxxxxx",
	}
	_, invocationErr := client.Experiences.Delete(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestExperiencesDeleteWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestExperiencesDeleteWithWireMock", "DELETE", "/experiences/exp_xxxxxxxxxxxxxx", nil, 1)
}

func TestExperiencesUpdateWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewWhop(
		option.WithBaseURL(WireMockBaseURL),
		option.WithToken("test-token"),
	)
	request := &whopsdk.UpdateExperiencesRequest{
		ID: "exp_xxxxxxxxxxxxxx",
	}
	_, invocationErr := client.Experiences.Update(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestExperiencesUpdateWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestExperiencesUpdateWithWireMock", "PATCH", "/experiences/exp_xxxxxxxxxxxxxx", nil, 1)
}

func TestExperiencesAttachWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewWhop(
		option.WithBaseURL(WireMockBaseURL),
		option.WithToken("test-token"),
	)
	request := &whopsdk.AttachExperiencesRequest{
		ID:        "exp_xxxxxxxxxxxxxx",
		ProductID: "prod_xxxxxxxxxxxxx",
	}
	_, invocationErr := client.Experiences.Attach(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestExperiencesAttachWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestExperiencesAttachWithWireMock", "POST", "/experiences/exp_xxxxxxxxxxxxxx/attach", nil, 1)
}

func TestExperiencesDetachWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewWhop(
		option.WithBaseURL(WireMockBaseURL),
		option.WithToken("test-token"),
	)
	request := &whopsdk.DetachExperiencesRequest{
		ID:        "exp_xxxxxxxxxxxxxx",
		ProductID: "prod_xxxxxxxxxxxxx",
	}
	_, invocationErr := client.Experiences.Detach(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestExperiencesDetachWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestExperiencesDetachWithWireMock", "POST", "/experiences/exp_xxxxxxxxxxxxxx/detach", nil, 1)
}

func TestExperiencesDuplicateWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewWhop(
		option.WithBaseURL(WireMockBaseURL),
		option.WithToken("test-token"),
	)
	request := &whopsdk.DuplicateExperiencesRequest{
		ID: "exp_xxxxxxxxxxxxxx",
	}
	_, invocationErr := client.Experiences.Duplicate(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestExperiencesDuplicateWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestExperiencesDuplicateWithWireMock", "POST", "/experiences/exp_xxxxxxxxxxxxxx/duplicate", nil, 1)
}
